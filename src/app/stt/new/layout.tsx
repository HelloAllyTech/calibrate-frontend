import { Metadata } from "next";

export const metadata: Metadata = {
  title: "New STT Evaluation | Calibrate",
};

export default function NewSTTLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
